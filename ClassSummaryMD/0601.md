# 0601 수업내용정리

## 복습
추후 내용추가 or 챕터별 정리본 통합

___

## 0601 진도

### 1. 객체지향프로그래밍
- 핵심 지향점
  - 프로그램의 부품화: 데이터와 이를 처리하는 메서드를 객체라는 하나의 단위로 묶어 관리
  - 재사용성과 유지보수성이 높음

#### 1.1 객체, 클래스, 인스턴스
- 객체: 유무형의 모든 실체
  - 고유한 상태와 동작을 가짐
  - 상태 -> 필드, 동작 -> 메소드
- 클래스: 객체를 생성하기 위한 설계도
  - 객체의 형태와 기능을 자바 코드로 사전에 명시해둔 규격, 메모리에 실체는 없음
- 인스턴스: 클래스라는 설계도를 바탕으로 'new'연산자를 사용하여 힙 메모리에 생성한 실체

#### 1.2 클래스의 구성요소
- 필드: 객체의 상태, 속성을 정의한 변수
- 메서드: 객체의 동작, 행위, 기능을 담고있는 코드 블럭
- 생성자: 객체가 new로 생성될 때 초기 세팅을 담당

```java
public class 클래스명 {
    // 필드
    public 타입 변수명;
    
    // 생성자
    public 클래스명(매개변수타입 매개변수명, ...) {
        // 초기 세팅코드
    }
}
```